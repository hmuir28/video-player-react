import styled from 'styled-components';

const StyledNavbar = styled.div`
  .nav-wrapper {
    background: #696969;
  }

  .dropdown-content {
    li {
      a {
        color: #888;
      }
    }
  }
`;

export default StyledNavbar;
